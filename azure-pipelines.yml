trigger:
- "*"

pool:
  vmImage: ubuntu-latest

stages:
- stage: Test

  jobs:
  - job: Hello

    steps:
    - script: |
        echo "Pipeline is working"
        echo "Branch = $(Build.SourceBranchName)"